#列表拼接
lst1 = [1, 2, 3]
lst2 = [4, 5, 6]
new_lst = lst1 + lst2
print(new_lst)  # [1, 2, 3, 4, 5, 6]
# 原列表不会被修改
print(lst1)     # [1, 2, 3]

#注意点
#1。只能 列表 + 列表，不能和单个元素、字符串、元组相加：
# 报错
# [1,2] + 3
# [1,2] + "abc"
#2.lst1 + lst2 生成新列表，原列表保持不变。
#3.追加单个元素优先用 append()，批量追加优先 extend()。

